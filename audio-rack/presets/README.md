# Audio Rack presets

這裡保存可重跑的研究 preset 定義。preset 只描述參數與驗證狀態，不代表已安裝的 VST chain 或人工批准的聲音風格。

目前沒有 `approved` preset。`seed-vc-neutral.json` 只建立 bypass／full-chain 對照契約；每個 plugin 的版本、實測 Δ latency 與人工聽測仍是 `WAITING`。
